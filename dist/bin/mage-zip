#!/usr/bin/env bash

set -euo pipefail

echo "Archiving Magento2 directories app, dev, generated, lib, and vendor..."

for folder in app dev generated lib vendor; do tar -zcf $folder.tar.gz $folder; echo "$folder zipped"; done;

echo "ARCHIVING COMPLETE!"

exit 0
